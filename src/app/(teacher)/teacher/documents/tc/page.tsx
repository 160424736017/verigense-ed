import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function TeacherTransferCertificatePage() {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Transfer Certificate</CardTitle>
          <CardDescription>Generate transfer certificates for students</CardDescription>
        </CardHeader>
        <CardContent>
          <p>Transfer certificate generation features for teachers will be implemented here.</p>
        </CardContent>
      </Card>
    </div>
  )
}